rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only a signed-in user (you) can read/write any data.
    // Since this is a single-owner shop app, any authenticated user = you.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
